services:
  - type: web
    name: chartwise-api
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install fastapi uvicorn
    startCommand: uvicorn main_minimal:app --host 0.0.0.0 --port 8000
    healthCheckPath: /health
